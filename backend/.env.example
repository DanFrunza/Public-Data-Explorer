# Database connection for Docker Compose network
DB_HOST=postgres
DB_USER=postgres
DB_PASS=postgres
DB_NAME=public_data

# Comma-separated list of allowed frontend origins for CORS
# Example: http://localhost:5173,http://localhost:3000
CORS_ALLOWED_ORIGINS=
