# --- Database connection (Pipeline) ---
# These override the defaults in code. For local docker-compose, the defaults
# match the postgres service (host "postgres", user/password "postgres").
# Set real values in a local .env (not committed) and reference it from compose.

DB_HOST=postgres
DB_USER=postgres
DB_PASS=postgres
DB_NAME=public_data

# --- World Bank ingestion ---
# API base (optional; defaults to https://api.worldbank.org/v2)
WB_BASE=
# Optional end year override for backfills; leave empty to use current year.
WB_END_YEAR=

# Run all jobs once on container start (useful for manual tests/backfills)
RUN_JOBS_ON_START=false

# Scheduler timezone
TZ=UTC
